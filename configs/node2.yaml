# ─── bgpd-agent configuration — Node 2 ───────────────────────────────────────
# Datacenter: DC2 / Provider B
# Assigned at runtime: 203.0.113.64/26  WG IP: 10.100.0.3

log_level: info

node_id: "node2"

core:
  public_ip: "YOUR_CORE_PUBLIC_IP"
  api_port: 8080
  api_key: "YOUR_SECRET_API_KEY_HERE"

wireguard:
  interface: "wg0"
  public_ip: "YOUR_NODE2_PUBLIC_IP"
  listen_port: 51820
  mtu: 1420
  private_key: ""

bgp:
  hold_time: 90
  keepalive: 30

heartbeat_interval: "30s"
